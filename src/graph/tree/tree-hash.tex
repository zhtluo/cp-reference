\mintinline{cpp}|A[n]| is the hash of the sub-tree with root \mintinline{cpp}|n|.

\mintinline{cpp}|B[n]| is the hash of the whole tree with root \mintinline{cpp}|n|.

\inputminted{cpp}{src/graph/tree/tree-hash.cpp.com}
