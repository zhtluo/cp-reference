Find the immediate dominator (\mintinline{cpp}|idom[]|) of each node, \mintinline{cpp}|idom[x]| will be $x$ if $x$ does not have a dominator, and will be $-1$ if $x$ is not reachable from $s$. 

\inputminted{cpp}{src/graph/cut/dominator-tree.cpp.com}

